[project]
name = "common"
version = "0.0.0"  # never released; live-at-head, kept at 0.0.0 on purpose
description = "Shared helpers for the Python services (live at head)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/common"]
