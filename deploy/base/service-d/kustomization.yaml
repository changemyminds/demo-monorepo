apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
images:
  - name: app
    newName: ghcr.io/example/service-d
    # newTag is set per-environment in the overlay / at deploy time.
